# Replace placeholders locally with your Supabase PostgreSQL connection details.
# URL-encode special characters in the username and password.
DATABASE_URL=postgresql+psycopg://USERNAME:PASSWORD@HOST:5432/DATABASE?sslmode=require
